import turtle
colors = ['red', 'purple', 'blue', 'green', 'orange', 'yellow']
t = turtle.Turtle()
turtle.bgcolor('black')
t.speed(7000)
for x in range(360):
    t.pencolor(colors[x%6])
    t.width(1)
    t.forward(x)
    t.left(59)
